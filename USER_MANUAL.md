# SailWatch Track 用户使用手册

## 1. 使用前准备

使用应用前请准备：

- 已安装 SailWatch Track 的华为手表。
- 已充足充电并开启蓝牙、定位功能的手表。
- 名称以 `WT901` 开头的 BLE 姿态传感器。
- 在线模式还需要可用网络和正确配置的训练服务器。

建议先开启 WT901 传感器，再启动手表应用。传感器不要同时连接其他手机或手表。

## 2. 首次启动与授权

首次打开应用时，系统会请求健康数据、定位、加速度计、蓝牙、网络和后台运行权限。请允许全部权限，否则可能出现以下情况：

- 无法搜索或连接 WT901。
- 船速、GPS 轨迹为空。
- 心率数据为 0。
- 熄屏或进入后台后记录中断。

授权完成后进入“训练模式”页面，可选择“连接服务器”“离线模式”或“历史报告”。

## 3. 连接 WT901

在线模式和离线模式都会进入 WT901 选择页面，两种模式均可点击“跳过”，在不连接姿态传感器的情况下继续训练。

1. 等待应用自动扫描，或点击“扫描”。
2. 在列表中查找名称以 `WT901` 开头的设备。
3. 通过设备名称、扫描地址和 RSSI 区分多个传感器。RSSI 越接近 0，通常代表距离越近。扫描地址来自系统，不保证等于设备标签上的 MAC，使用隐私随机地址时还可能变化。
4. 点击目标设备，页面状态会变为正在连接。
5. 选择设备后点击底部“进入”。

扫描不到设备时：

1. 确认 WT901 已开机并靠近手表。
2. 确认手表蓝牙已开启。
3. 确认蓝牙和定位权限已授权。
4. 点击“停止”，等待片刻后重新点击“扫描”。
5. 确认传感器未连接其他设备。

## 4. 在线训练

### 4.1 进入在线模式

1. 在训练模式页面点击“连接服务器”。
2. 选择教练艇编号。
3. 选择训练级别。
4. 选择运动员分组。
5. 选择并连接 WT901，或点击“跳过”以无姿态传感器方式进入。
6. 点击“进入”打开在线训练页面。

首次选择完成后，教练艇、训练级别和运动员分组会保存到应用沙箱。以后点击“连接服务器”将直接进入 WT901 选择页，不会保存蓝牙设备。

在线实时页面提供“重新设置”按钮。未开始训练时可点击该按钮重新选择并覆盖配置；训练开始后按钮自动禁用，需先长按停止训练。

### 4.2 开始与停止

- 点击“开始”后，应用开始通过 MQTT 上报航行数据。
- 训练过程中请保持应用运行，并确认网络、GPS 和 WT901 状态正常。
- 停止训练时长按停止按钮约 2 秒，圆环走完后完成停止。

MQTT 断线后，应用会每 3 秒检查并尝试重新连接。单次连接最多等待约 5 秒，重连成功后会恢复风数据订阅，并继续发送内存队列中的待发消息。

## 5. 离线训练

### 5.1 进入离线模式

1. 在训练模式页面点击“离线模式”。
2. 选择并连接 WT901 后点击“进入”，或点击“跳过”以无姿态传感器方式训练。
3. 打开离线训练页面。

离线模式不会选择教练艇、训练级别或运动员，也不会连接 MQTT。数据仅保存在手表应用沙箱中。

### 5.2 页面内容

离线训练页面显示：

- 离线模式。
- 姿态传感器连接状态（小圆点：绿色为已连接，红色为断开，灰色为已跳过）。
- 历史记录入口。
- 当前船速，单位为节（`kn`）。
- 本次航行时间。
- 最近 1 分钟平均倾角和实时心率；无有效姿态数据时倾角显示 `---`。
- 开始记录或长按停止按钮。

### 5.3 开始记录

点击“开始记录”。应用每秒保存一次数据快照，包括：

- WT901 加速度、角速度、姿态角和原始数据。
- GPS 经纬度、船速、航向和定位精度。
- 手表心率。

记录期间“历史记录”按钮不可使用，防止离开记录页面造成误操作。

### 5.4 停止并生成报告

1. 持续按住“长按停止”约 2 秒。
2. 保持按住直到红色进度圆环走完。
3. 松开后应用停止记录并生成本次训练报告。

短按停止按钮不会结束记录。

## 6. 查看历史报告

可通过以下入口查看：

- 训练模式页面的“历史报告”。
- 离线模式未记录时的“历史记录”。

历史列表显示训练日期、训练时长和平均心率。点击一条记录可查看：

- 训练开始时间和时长。
- 样本数量。
- 平均、最高和最低心率。
- 心率折线图。
- 横滚倾角折线图，以及平均和最大倾角。
- 估算航行距离。

每条历史记录右侧提供“删除”按钮。第一次点击后按钮变为“确认”，再次点击会同时删除该训练的原始数据、报告和历史索引，删除后无法恢复。

报告图表会对数据点进行抽样，便于在手表小屏幕上显示；原始 GPS、心率和 IMU 数据仍按 JSONL 形式保存在应用沙箱中。最近 1 分钟平均倾角和报告倾角均保留横滚角的正负方向，报告曲线以 0° 基准线区分两侧倾斜。

未连接姿态传感器，或传感器连续 5 秒没有产生新数据时，该秒 JSONL 样本的 `imu` 字段保存为 `"-"`。这类样本不参与倾角均值、峰值和曲线计算。

## 7. 数据说明

- 船速显示单位为节（kn）。
- GPS 轨迹需要有效定位，首次定位建议在室外空旷区域等待一段时间。
- 心率需要正确佩戴手表并授予健康数据权限。
- WT901 数据需要传感器保持连接。
- 历史索引最多显示最近 50 份报告。
- 卸载应用会删除离线原始数据和历史报告。

## 8. 常见问题

### WT901 列表为空

检查传感器电源、手表蓝牙、应用权限和设备占用情况，然后停止并重新扫描。

### 点击设备后无法进入

确认已点击列表中的设备，并等待选择状态更新。必要时停止扫描后重新连接。

### 在线模式显示服务器断开

检查手表网络。应用会自动重试；持续失败时，需要管理员检查 MQTT 地址、端口、账号和服务器状态。

### 离线模式无法查看历史

记录期间历史入口会被禁用。请先长按停止并生成报告，再打开历史记录。

### 报告没有轨迹或心率

检查定位和健康数据权限。GPS 在室内可能无有效坐标，手表佩戴过松可能无法获得心率。

### 长按停止没有生效

按住按钮约 2 秒，不要在进度圆环完成前移开手指。

## 9. 使用注意事项

- 下水前确认手表、WT901 和网络状态。
- 长时间训练前保证手表和传感器电量充足。
- 离线记录结束后务必长按停止，确保报告正常生成。
- 重要训练结束后应及时导出或备份数据，避免卸载应用导致数据丢失。
- 应用用于训练数据采集，不应替代航行安全设备或专业医疗设备。
